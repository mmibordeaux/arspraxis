<%= content_for :title, 'Promotions' %>

<table class="table">
  <thead>
    <tr>
      <th>Année de sortie</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.year %></td>
        <td>
          <%= show_button [@program, group] %>
          <%= edit_button [@program, group] %>
          <%= destroy_button [@program, group] %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= create_button Program::Group %>
