<% content_for :title, "Bonjour #{current_user.first_name}" %>

<%= render 'users/publications/dashboard', publications: @publications, new_publication: @new_publication %>

<hr class="mt-5">
<%= render 'users/students/dashboard', student_in_groups: @student_in_groups %>

<% if @teacher_in_programs.any? %>
  <hr class="mt-5">
  <%= render 'programs/teachers/dashboard', teacher_in_programs: @teacher_in_programs %>
<% end %>

<% if @manager_of_referentials.any? %>
  <hr class="mt-5">
  <%= render 'referentials/managers/dashboard', manager_of_referentials: @manager_of_referentials %>
<% end %>
