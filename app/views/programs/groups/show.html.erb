<%= content_for :title, "#{@group}" %>

<%= edit_button [@program, @group] %>

<%= link_to 'Demander l\'inscription', new_user_student_path(user_id: current_user, program_group_id: @group), class: button_classes %>
