<% if @referential && @referential.image.attached? && @referential.image.variable? %>
  <style media="screen">
    body {
      background-image: asset-url('<%= url_for @referential.image.variant(resize: '1920', quality: 80) %>') !important;
    }
  </style>
<% end %>
