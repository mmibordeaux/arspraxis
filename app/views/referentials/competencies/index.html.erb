<%= content_for :title, 'Compétences' %>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Short name</th>
      <th>Description</th>
      <th>Number</th>
      <th>Referential</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @competencies.each do |competency| %>
      <tr>
        <td><%= competency.name %></td>
        <td><%= competency.short_name %></td>
        <td><%= competency.description %></td>
        <td><%= competency.number %></td>
        <td><%= competency.referential %></td>
        <td><%= link_to 'Show', referential_competency_path(@referential, competency) %></td>
        <td><%= link_to 'Edit', edit_referential_competency_path(@referential, competency) %></td>
        <td><%= link_to 'Destroy', referential_competency_path(@referential, competency), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= create_button Referential::Competency %>
